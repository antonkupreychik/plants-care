spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    validate-on-migrate: false

logging:
  level:
    org.hibernate.SQL: WARN
    com.plantcare.bot: DEBUG
