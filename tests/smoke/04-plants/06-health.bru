meta {
  name: Plants – health (may be insufficient data)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/plants/{{plantId}}/health
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
