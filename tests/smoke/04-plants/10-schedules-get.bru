meta {
  name: Plants – get schedules
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/plants/{{plantId}}/schedules
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
