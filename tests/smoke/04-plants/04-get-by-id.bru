meta {
  name: Plants – get by id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/plants/{{plantId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{plantId}}
  res.body.name: eq Smoke Test Ficus
}
