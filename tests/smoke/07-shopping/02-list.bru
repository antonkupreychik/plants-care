meta {
  name: Shopping – list
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/shopping
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
