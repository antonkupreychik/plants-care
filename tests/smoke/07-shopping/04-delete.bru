meta {
  name: Shopping – delete item
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/shopping/{{shoppingItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
