meta {
  name: Care Events – delete
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/care-events/{{careEventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
