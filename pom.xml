<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.plantcare</groupId>
    <artifactId>plants-care</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>plants-care</name>
    <description>Telegram bot for plant care reminders</description>

    <properties>
        <java.version>21</java.version>

        <!-- Вариант -hibernate-73 3.15.4 собран против Hibernate 7.4.0.Final, что совпадает
             с Boot 4.1 (7.4.1). Вариант -70 таргетит 7.0.10 и падает на 7.4 с
             IncompatibleClassChangeError (JsonJavaTypeDescriptor overrides final method). -->
        <hypersistence-utils.version>3.15.4</hypersistence-utils.version>
        <springdoc.version>3.1.0</springdoc.version>
        <lombok.version>1.18.46</lombok.version>
        <jacoco.version>0.8.14</jacoco.version>
        <openapi-generator.version>7.10.0</openapi-generator.version>
        <swagger-annotations.version>2.2.25</swagger-annotations.version>
        <!-- Issue #127: ArchUnit — проверка границ модульного монолита (core ∌ bot/admin/api). -->
        <archunit.version>1.5.0</archunit.version>
        <!-- Issue #114: Sentry 8.x. Артефакт sentry-spring-boot-4-starter собран против
             Boot 4.1.0; прежний -jakarta-вариант даже в 8.53.0 таргетит Boot 3.5.x. -->
        <sentry.version>8.53.0</sentry.version>
        <!-- Эпик #277 (фаза 1, #279): ShedLock 7.x — линия под Spring 7 / Boot 4.x. -->
        <shedlock.version>7.8.0</shedlock.version>
        <!-- Issue #90 (Slice A): AWS SDK v2 для S3-совместимого object storage (Railway).
             Лёгкий sync-клиент url-connection-client вместо netty-async (см. зависимости ниже). -->
        <awssdk.version>2.30.18</awssdk.version>
        <!-- Issue #176 (#167b): Firebase Admin SDK для FCM HTTP v1 (ADR-016). Единый шлюз
             push для iOS+Android, без APNs и legacy server-key. Тяжёлый транзитив
             (gRPC, google-auth, protobuf) — прогрев ~/.m2 обязателен для офлайн-сборки. -->
        <firebase-admin.version>9.9.0</firebase-admin.version>

        <!-- Sonar -->
        <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
        <sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
        <sonar.jacoco.reportPath>${project.build.directory}/jacoco.exec</sonar.jacoco.reportPath>
        <sonar.language>java</sonar.language>
        <sonar.coverage.assertions.enabled>true</sonar.coverage.assertions.enabled>
    </properties>

    <dependencies>
        <!-- Spring Boot starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Issue #115: бизнес-метрики через Prometheus scrape /actuator/prometheus.
             Версия наследуется из spring-boot BOM, явно не пиннингуем. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Issue #114: агрегация ошибок в Sentry. Единственный канал error-aggregation
             на проде после запуска REST API (#84) и mobile (#88+). Обоснование в issue. -->
        <dependency>
            <groupId>io.sentry</groupId>
            <artifactId>sentry-spring-boot-4-starter</artifactId>
            <version>${sentry.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Issue #88: верификация наших JWT (resource server) и Apple/Google JWKS. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Issue #88: отправка email magic-link через SMTP. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <dependency>
            <groupId>nz.net.ultraq.thymeleaf</groupId>
            <artifactId>thymeleaf-layout-dialect</artifactId>
        </dependency>

        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>

        <!-- Issue #92: Rate limiting per-IP and per-user.
             Implemented with a sliding-window counter backed by Caffeine (already on classpath).
             bucket4j was initially planned but is not yet cached locally; Caffeine provides
             equivalent in-memory sliding-window semantics with zero new transitive deps. -->

        <!-- Подготовка к Spring Boot 4: взят «голый» telegrambots-longpolling вместо
             spring-boot-стартера. Стартер собран против Boot 3.5.x и тянет его
             автоконфигурацию (TelegramBotStarterConfiguration), которой мы не пользуемся:
             TelegramBotsLongPollingApplication создаётся руками в TelegramBotConfig,
             а TelegramClient — в AdminTelegramConfig. Импортов из пакета
             ...longpolling.starter в коде нет. -->
        <dependency>
            <groupId>org.telegram</groupId>
            <artifactId>telegrambots-longpolling</artifactId>
            <version>9.5.0</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.telegram</groupId>
            <artifactId>telegrambots-client</artifactId>
            <version>9.5.0</version>
        </dependency>
        <dependency>
            <groupId>net.iakovlev</groupId>
            <artifactId>timeshape</artifactId>
            <version>2025b.28</version>
            <scope>compile</scope>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Boot 4: автоконфигурация Flyway вынесена в модуль spring-boot-flyway.
             Голый flyway-core её больше НЕ приносит — на Boot 4 миграции просто
             перестают накатываться молча, а Hibernate падает на validate.
             Стартер тянет и flyway-core, и автоконфигурацию. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- JSONB support -->
        <dependency>
            <groupId>io.hypersistence</groupId>
            <artifactId>hypersistence-utils-hibernate-73</artifactId>
            <version>${hypersistence-utils.version}</version>
        </dependency>

        <!-- OpenAPI / Swagger UI (2.8.x совместим со Spring Boot 3.5.x) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Runtime deps для кода, сгенерированного openapi-generator (spring).
             jackson-databind-nullable убран: JsonNullable не используется ни в src/,
             ни в 134 файлах target/generated-sources. Держал Jackson 2 на класспасе
             без причины — мешает переходу на Jackson 3. -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations</artifactId>
            <version>${swagger-annotations.version}</version>
        </dependency>

        <!-- Issue #279, эпик #277 фаза 1: ShedLock на JDBC (Postgres) — distributed
             lock для @Scheduled, анти-дубли напоминаний при multi-instance.
             Намеренно НЕ Redis-provider: путь напоминаний не должен зависеть от Redis. -->
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-spring</artifactId>
            <version>${shedlock.version}</version>
        </dependency>
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-provider-jdbc-template</artifactId>
            <version>${shedlock.version}</version>
        </dependency>

        <!-- Issue #278, эпик #277 фаза 0: Redis (Lettuce). Пререквизит для rate-limit (#277b),
             cache (#277c) и токен-хранилища (#277d). Намеренно Lettuce (по умолчанию
             в spring-boot-starter-data-redis): неблокирующий клиент, thread-safe
             shared connection, хорошо работает с виртуальными потоками Java 21. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Issue #90 (Slice A): S3-совместимое object storage (Railway) через AWS SDK v2.
             netty-nio-client исключён в пользу url-connection-client (лёгкий sync-HTTP),
             чтобы не тащить netty-async — нам достаточно простого синхронного клиента. -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>${awssdk.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
            <version>${awssdk.version}</version>
        </dependency>

        <!-- Issue #176 (#167b): FCM-шлюз через Firebase Admin SDK (ADR-016), HTTP v1.
             Реализация PushSender за тумблером push.enabled=true; при выключенном
             тумблере SDK не инициализируется (NoopPushSender). -->
        <dependency>
            <groupId>com.google.firebase</groupId>
            <artifactId>firebase-admin</artifactId>
            <version>${firebase-admin.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Boot 4 разнёс тестовые срезы по отдельным модулям: @WebMvcTest/@AutoConfigureMockMvc
             уехали из spring-boot-test-autoconfigure в spring-boot-webmvc-test,
             @DataJpaTest — в spring-boot-data-jpa-test. Без этих стартеров срезы не компилируются. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Boot 4: SecurityMockMvcAutoConfiguration (подключение springSecurity() к MockMvc)
             вынесена в модуль spring-boot-security-test и НЕ входит в starter-webmvc-test.
             Без неё security-фильтры в @WebMvcTest не отрабатывают, request.getUserPrincipal()
             пуст, и параметр метода Authentication приезжает в контроллер как null. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Версия Testcontainers намеренно не пинуется: её задаёт BOM spring-boot-dependencies
             (импортирует testcontainers-bom). В Testcontainers 2.0 артефакты переименованы:
             postgresql -> testcontainers-postgresql, junit-jupiter -> testcontainers-junit-jupiter. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>


        <!-- Issue #127: ArchUnit — фитнес-функция границ модульного монолита. -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- OpenAPI Generator: spec-first, генерируем только interfaces + модели -->
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${openapi-generator.version}</version>
                <executions>
                    <execution>
                        <id>generate-api</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${project.basedir}/src/main/resources/openapi/openapi.yaml</inputSpec>
                            <generatorName>spring</generatorName>
                            <output>${project.build.directory}/generated-sources/openapi</output>
                            <apiPackage>com.plantcare.api.generated</apiPackage>
                            <modelPackage>com.plantcare.api.generated.model</modelPackage>
                            <invokerPackage>com.plantcare.api.generated</invokerPackage>
                            <generateApiTests>false</generateApiTests>
                            <generateModelTests>false</generateModelTests>
                            <generateApiDocumentation>false</generateApiDocumentation>
                            <generateModelDocumentation>false</generateModelDocumentation>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <library>spring-boot</library>
                                <useSpringBoot3>true</useSpringBoot3>
                                <useJakartaEe>true</useJakartaEe>
                                <interfaceOnly>true</interfaceOnly>
                                <useTags>true</useTags>
                                <skipDefaultInterface>true</skipDefaultInterface>
                                <useResponseEntity>false</useResponseEntity>
                                <documentationProvider>springdoc</documentationProvider>
                                <annotationLibrary>swagger2</annotationLibrary>
                                <openApiNullable>false</openApiNullable>
                                <useBeanValidation>true</useBeanValidation>
                                <dateLibrary>java8</dateLibrary>
                                <serializableModel>false</serializableModel>
                                <hideGenerationTimestamp>true</hideGenerationTimestamp>
                                <containerDefaultToNull>true</containerDefaultToNull>
                            </configOptions>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Java 24 + Lombok annotation processor -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- JaCoCo coverage -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <excludes>
                                <exclude>com/plantcare/api/generated/**/*</exclude>
                            </excludes>
                        </configuration>
                    </execution>

                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <excludes>
                                <exclude>com/plantcare/api/generated/**/*</exclude>
                            </excludes>
                        </configuration>
                    </execution>

                    <execution>
                        <id>check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <excludes>
                                <exclude>com/plantcare/api/generated/**/*</exclude>
                            </excludes>
                            <rules>
                                <rule>
                                    <!--
                                      Пороги подняты вместе с волной тестов этой ветки: было 0.49 по строкам
                                      при фактических 57%, то есть гейт отставал от реальности и ничего не ловил.
                                      Фактически на момент правки: строки 84.15%, ветки 71.92% (2930 тестов).
                                      Пороги выставлены с запасом ~4 п.п. вниз от факта, чтобы обычные колебания
                                      не роняли CI, но просадка покрытия ловилась.

                                      BRANCH добавлен намеренно: раньше гейт стоял только на строках, а ветки
                                      покрыты хуже (49% против 57% до этой работы). Для логики расписаний,
                                      таймзон и условий отправки непокрытая ветка опаснее непокрытой строки.

                                      Сгенерированный com.plantcare.api.generated.** исключён из подсчёта.
                                    -->
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.68</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- SonarQube -->
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>5.0.0.4389</version>
            </plugin>

            <!-- Spring Boot -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>